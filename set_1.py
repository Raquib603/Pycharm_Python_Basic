#set doesnt use key
#duplicate values are not allowed
#Set items are unordered, unchangeable, and do not allow duplicate values.


x = {1, 2, 4, 4, 'apple'}
print(x)
print(type(x))

#count the elements in a set
print(len(x))

#we also can convert something into set by set constructor set()